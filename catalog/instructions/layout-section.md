## Layout

<!-- One line per top-level directory: what lives there and, if it is generated,
     the command and source that produce it. -->

- `<dir>/` - <what it holds>
- `<dir>/` - generated from `<source>` by `<command>`; never hand-edited

## Common tasks

<!-- The commands a contributor runs most, each with one line on what it does. -->

- `<command>` - <what it does>
- `<guard command>` - proves the house rule holds; run before every PR
