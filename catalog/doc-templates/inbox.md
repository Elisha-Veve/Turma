<!-- TEMPLATE: seeds docs/inbox.md, appended to by turma:inbox. Each item is one line in
     the user's own words, dated when captured. An item leaves this file when
     turma:define writes its problem doc - the problem doc then owns it. -->
# Inbox

Issues noted but not yet defined. Promote one with `/turma:inbox promote <n>`.

- <YYYY-MM-DD> <THE ISSUE, AS THE USER PUT IT>
