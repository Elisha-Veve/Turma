{
  "_comment": "Merged into a repo's .claude/settings.json by turma:bootstrap. Wires the two optimizer-loop nudges. The git post-commit hook (hooks/commit-queue.sh) is installed into .git/hooks/post-commit, not here. Hook scripts are copied into <repo>/.claude/hooks/ so scripts/check.mjs can verify them against the catalog.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/after-commit-nudge.sh", "timeout": 10 }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/optimizer-nudge.sh", "timeout": 10 }
        ]
      }
    ]
  }
}
