{
  "_comment": "Merged into a repo's .claude/settings.json by turma:bootstrap. Keeps secrets out of context. Extend the deny list with any other secret-bearing paths the repo has.",
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.local)",
      "Read(./.env.*.local)",
      "Read(./.env.production)"
    ]
  }
}
